import UIOnlyModulePage from './UIOnlyModulePage'

export default function DataManagementPage() {
  return <UIOnlyModulePage title="Project Data Repository" subtitle="UI-only data repository layout for LAS, seismic, reports, images, and tables." accent="#38BDF8" />
}
