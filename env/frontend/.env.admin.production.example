# Drake AI admin dashboard production build
# Copy to frontend/.env.production before building the admin dashboard,
# or pass these as Docker build args in docker-compose.admin.yml.

VITE_PORTAL_MODE=admin
VITE_API_URL=https://api.your-domain.com
ADMIN_DASHBOARD_PORT=8080
