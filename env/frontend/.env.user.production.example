# Drake AI user dashboard production build
# Copy to frontend/.env.production before building the user dashboard,
# or pass these as Docker build args in docker-compose.user.yml.

VITE_PORTAL_MODE=user
VITE_API_URL=https://api.your-domain.com
USER_DASHBOARD_PORT=80
